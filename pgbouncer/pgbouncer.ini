[databases]
* = host=postgres port=5432 user=${PGBOUNCER_USER} password=${PGBOUNCER_PASSWORD}

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = md5
auth_file = /opt/bitnami/pgbouncer/conf/userlist.txt
admin_users = ${PGBOUNCER_USER}
