FROM bitnami/postgresql:16

# Ensure PostgreSQL listens only internally
ENV POSTGRESQL_LISTEN_ADDRESSES=127.0.0.1

# Declare volume (optional but recommended for clarity)
VOLUME ["/bitnami/postgresql"]
